


模拟图像`取样`、`量化`之后就变成了数字图像。
* 取样：对(x,y)进行离散化
* 量化：对f(x,y)进行离散化

代数运算、几何变换
* 代数运算
	- 加
	- 减
	- 乘
* 几何变换
	- 平移
	- 放缩
	- 旋转
	- 水平\垂直镜像
	- 变形变换


* 向前映射法 : 适用于平移、镜像
* 向后映射法 : 适用于旋转、放缩、复杂度的空间变换
	- 放大变换：输入图像小，输出图像大，如果用向前映射法，输出图像就会有空洞
	- 缩小变换：输入图像大，输出图像消，如果用向前映射法，就会多个像素映射到同一个位置上
	- 旋转变换：因为cos、sin是小数，所以四舍五入后，既可能多个像素映射到同一个位置上，也可能造成输出图像空洞
	- 所以使用向后映射，输出图像上的像素值在输入图像上进行`重采样`得到的，所以可以把插值理解成重采样
http://blog.csdn.net/hxm84/article/details/47313381


[[插值方法]]
* 最近邻内插
* 线性内插
* 双线性内插
* 高阶插值


